
/*
 * Creates a default [html] type of page, and returns to caller.
 */

/*
 * Returning page to caller.
 */
return
  template:/modules/system42/apps/CMS/page-templates/default.hl
  html:@"<p>Foo, and then a dash of bar too, I guess ...?</p>"

